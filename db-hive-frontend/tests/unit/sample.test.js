const assert = require('assert');

function run() {
  // Basic assertion to exercise the test runner in CI for the frontend skeleton
  assert.strictEqual(1 + 1, 2, 'basic arithmetic should work');
  console.log('frontend sample tests passed');
}

run();
