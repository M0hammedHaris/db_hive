# Story 1.1: Repository & CI/CD Scaffold

Status: Draft

## Story

**As a** developer
**I want** a polyrepo skeleton (frontend, backend, infra tooling) and a CI pipeline that runs unit tests and deploys a canary branch to staging (Vercel for frontend; serverless functions for backend)
**So that** we can validate an end-to-end canary flow, reduce onboarding friction, and provide a repeatable baseline for subsequent stories.

## Acceptance Criteria

1. Repos created with README and minimal directory layout.
2. CI pipeline runs unit tests and deploys a canary build on merge to `canary` branch.
3. A public staging health endpoint returns 200 and a JSON status payload.
4. Basic contribution & deploy instructions documented in README.

## Context & Pre-conditions

- This is the first story in Epic 1. No previous story implementation files found in `docs/stories/`. (No prior Dev Agent Records available.)

---

## Decision & Approvals

Status: PENDING

- Repo model decision: [PENDING — choose one of the options below and record the approver(s) and date]
  - Option A — Polyrepo (recommended by PRD): create separate repositories for frontend, backend, and infra. Suggested repo names (examples, confirm before creating): `db-hive-frontend`, `db-hive-backend`, `db-hive-infra`.
  - Option B — Monorepo (recommended by Architecture guidance): single repository with top-level directories `frontend/`, `backend/`, `infra/` (use Turborepo or workspace tooling for builds and CI).

- Decision rationale: Polyrepo simplifies separate deployments and repo ownership; Monorepo simplifies cross-cutting changes and single CI configuration. Confirm with Product + Architecture.

- Decision approvers (fill in):
  - Product Owner: [name]  Date: [YYYY-MM-DD]
  - Architect/Tech Lead: [name]  Date: [YYYY-MM-DD]
  - Infra/DevOps: [name]  Date: [YYYY-MM-DD]

NOTE: Implementation MUST not proceed past Task 1 until the repo model and repo names/locations are recorded and approved.


## Dev Notes (ARCHITECTURE-ONLY)

Note: the sections below contain only information extracted from the project's architecture documents. Do NOT invent or assume additional technical details.

### System overview (relevant items)

- Frontend: Next.js application intended to be deployed to Vercel / CDN. [Source: docs/architecture.md#System overview (high level)]
- Backend: Serverless query-proxy functions (AWS Lambda / Vercel Serverless) are the expected backend runtime for serverless endpoints and the health-check. [Source: docs/architecture.md#System overview (high level)]
- CI/CD recommendation: GitHub Actions / Turborepo (or similar) with canary deployments and feature flags is the recommended CI approach. [Source: docs/architecture.md#System overview (high level)]

### Deployment, canary & rollback guidance

- Canary deployments should be short-lived and feature-flagged; health checks and telemetry must be confirmed for canary routes prior to promotion. [Source: docs/architecture.md#Deployment, canary & rollback strategy]
- Health checks should validate basic infra posture (e.g., DB reachable, env OK) and return a JSON metadata/status payload for staging routes. [Source: docs/architecture.md#Deployment, canary & rollback strategy]

### IaC / Project layout guidance

- Recommended top-level layout (used as guidance for minimal directory layout): `infra/`, `frontend/`, `backend/`, `tests/`, `docs/`. This layout is the canonical project layout recommended by architecture. [Source: docs/architecture.md#IaC skeleton & recommended project layout]

### Testing guidance (relevant for CI)

- Unit tests are required for frontend and backend modules; integration tests for the canary path should run against staging infrastructure where possible. Playwright E2E tests are recommended for the canary path (create connection → test → create query → run → export). [Source: docs/architecture.md#Testing strategy]

### Observability & telemetry requirements

- Telemetry and structured logs are expected to be present for the canary flow; the architecture calls for telemetry events and centralized logs (CloudWatch / Vercel logs, Sentry) to validate canary health. [Source: docs/architecture.md#Observability & SLOs]

### Security & secrets

- Secrets should be stored with a managed secret store (AWS Secrets Manager or Vercel environment variables + encryption) and must not be stored in plaintext. [Source: docs/architecture.md#Security & secrets model]

---

## Environment & Secrets (recommended names)

Add the following environment variables / secret names to your chosen secret store. Replace example values with project-specific values and document where they are stored (Vercel env / GitHub Secrets / AWS Secrets Manager).

- `VERCEL_TOKEN` — Vercel deploy token for frontend canary deployments.
- `CI_DEPLOY_KEY` — Private deploy key or token used by CI to deploy backend serverless functions.
- `NEON_DB_URL` — NeonDB connection URL for staging (used by health-check to validate DB reachability).
- `LOGGING_DSN` — Optional Sentry / logging DSN for centralized error reporting during canary runs.
- `CI_HEALTH_CHECK_URL` — CI-level parameter for the staging health endpoint (used in CI to validate canary deploys).

Document where each secret is stored and which team or role has access.


## Tasks / Subtasks

The tasks below are derived from the Epic's acceptance criteria and constrained to the project's architecture guidance. Each task references relevant architecture or epic source material.

- Task 1 (AC: 1): Create repository skeletons for the project (polyrepo as specified in PRD: separate repos for frontend, backend, infra). For each repo include a minimal directory layout based on the architecture guidance (`infra/`, `frontend/`, `backend/`, `tests/`, `docs/`).
  - Subtask 1.1: Initialize repositories and push an initial `README.md` and `CONTRIBUTING.md` for each repository. [Source: docs/prd_shards/epics/epic-1.md#Story-1.1]
  - Subtask 1.2: Add the minimal directory layout in each repo per architecture recommendation. [Source: docs/architecture.md#IaC skeleton & recommended project layout]

- Task 2 (AC: 2): Configure CI pipeline to run unit tests and deploy canary builds on merge to `canary` branch.
  - Subtask 2.1: Add a GitHub Actions workflow that runs on PR and on pushes to `canary` to execute unit tests and build artifacts. [Source: docs/architecture.md#System overview (high level)]
  - Subtask 2.2: Wire frontend build/deploy to Vercel canary route and backend serverless function deploy step to the chosen serverless platform (Vercel serverless or AWS Lambda), driven from the CI workflow. [Source: docs/architecture.md#System overview (high level)]
  - Subtask 2.3: Ensure CI exposes a step that runs automated health-check validation against the deployed canary (see Task 3). [Source: docs/architecture.md#Deployment, canary & rollback strategy]

- Task 3 (AC: 3): Implement a lightweight staging health endpoint and related CI validation.
  - Subtask 3.1: Create a serverless health-check endpoint (e.g., `GET /health`) in the backend service that returns a JSON payload with service metadata and basic infra checks (status). Keep implementation minimal for the canary. [Source: docs/architecture.md#Deployment, canary & rollback strategy]
  - Subtask 3.2: Add an automated CI step that invokes the staging health endpoint and fails the canary promotion if the health check does not return expected status/HTTP 200. [Source: docs/architecture.md#Deployment, canary & rollback strategy]

- Task 4 (AC: 4): Document contribution & deploy instructions and canary runbook in each repo's README.
  - Subtask 4.1: Add `README.md` content covering: how to run tests locally, CI expectations, how to deploy the canary (Vercel/Serverless link), and how to run the staging health-check locally. [Source: docs/prd_shards/epics/epic-1.md#Story-1.1]
  - Subtask 4.2: Add a short `deploy/` section or `docs/DEPLOY.md` that records canary promotion/rollback steps per architecture guidance. [Source: docs/architecture.md#Deployment, canary & rollback strategy]

- Task 5 (Cross-cutting): Testing & Observability integration
  - Subtask 5.1: Add a sample unit test for the health endpoint and configure the CI to run it. [Source: docs/architecture.md#Testing strategy]
  - Subtask 5.2: Annotate CI builds with telemetry hooks and ensure logs are routed to centralized logging as per architecture guidance. [Source: docs/architecture.md#Observability & SLOs]

---

## Health Endpoint Contract

This contract provides a concrete JSON schema and CI assertion for Acceptance Criteria #3.

- Path: `GET /health`
- Success condition (CI assertion): HTTP 200 and JSON body with `status === "ok"` and `services.db === "ok"`.
- Minimal response schema (examples):

  {
    "status": "ok",           // "ok" | "degraded" | "fail"
    "version": "v0.1",       // optional service version string
    "uptime": 12345,           // seconds
    "services": {
      "db": "ok"             // "ok" | "fail"
    },
    "timestamp": "2025-10-12T12:00:00Z"
  }

Source note: architecture mandates a JSON metadata/status payload for health checks and basic infra checks (db reachable, env OK). [Source: docs/architecture.md#Deployment, canary & rollback strategy]

CI example assertion (concept):

```bash
# Shell-style example for CI health check
curl -sSf "$CANARY_URL/health" | jq -e '.status == "ok" and .services.db == "ok"'
```

If the assertion fails the CI job must fail and block canary promotion.

## CI Workflow Blueprint

Create a CI workflow at `.github/workflows/canary.yml` with the following logical jobs (names are suggestions):

- `install-and-test` — checkout code, install dependencies, run unit tests (fail fast if tests fail).
- `build` — produce production build artifacts for frontend and backend.
- `deploy:frontend-canary` — deploy frontend build to Vercel canary route (requires `VERCEL_TOKEN` and project identifiers).
- `deploy:backend-canary` — deploy backend serverless functions to chosen provider (Vercel serverless or AWS Lambda; requires CI deploy credentials).
- `canary:health-check` — hit the staging canary URL and run the Health Endpoint Contract assertion; fail the workflow if health-check fails.
- `promote-or-halt` — on success, mark CI job as passed and optionally create a promotion PR or perform automated promotion per policy; on failure, fail and require manual investigation.

Notes:

- Use `ubuntu-latest` runners and standard actions: `actions/checkout`, `actions/setup-node`, caching steps for package manager, and provider-specific deployment actions.
- Replace test/build commands with the project's package manager commands (e.g., `npm ci && npm test && npm run build`).
- Store all deploy tokens and secrets in GitHub Actions secrets or the chosen secret store; do NOT commit tokens to the repo.

## Testing frameworks & file paths (implementation suggestions)

- Unit tests: use the project's chosen unit test runner for Node/TS (e.g., Jest) and place tests under `backend/tests/unit/**` and `frontend/tests/unit/**`.
- E2E tests (canary path): use Playwright and place tests under `tests/e2e/canary/**` that run the end-to-end canary flow in staging.
- Example minimal health unit test location: `backend/tests/unit/health.test.(ts|js)` asserting the `/health` route handler returns the contract above.

## Canary promotion / rollback guidance

- If the `canary:health-check` job fails, the workflow MUST fail and block promotion. Recommended actions on failure:
  - Stop automated promotion and alert the on-call / release owner.
  - Optionally toggle feature flags to disable new UI flows (if feature flags are in place).
  - If automatic rollback is enabled, the CI may trigger a rollback job that redeploys the previous stable artifacts; otherwise require manual rollback procedure by the release manager.
- Document the chosen promotion/rollback policy in `docs/DEPLOY.md` (see Task 4.2).


## Project Structure Notes (conflicts & decisions required)

- PRD explicitly describes a polyrepo approach for the MVP (separate frontend/backend/infra repos) while the architecture document provides a recommended top-level layout (`infra/`, `frontend/`, `backend/`, `tests/`, `docs/`) that is commonly used for a monorepo. The acceptance criteria for this story reference "Repos created" (plural), which implies a polyrepo approach per PRD. This is a project-level decision that should be confirmed by Product/Architecture before implementation proceeds. [Sources: docs/prd.md#Technical Assumptions; docs/architecture.md#IaC skeleton & recommended project layout]

---

## Testing & Validation Notes

- CI must run unit tests on PRs and run a canary deployment on `canary` branch pushes. Integration tests for the canary path are recommended to run in staging as part of the canary validation. [Source: docs/architecture.md#Testing strategy]

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-12 | v0.1 | Initial draft story created by Scrum Master (generator) | GitHub Copilot |
